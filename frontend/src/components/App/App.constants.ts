const ENTITY_FREQUENCIES: number[] = [];
for (let i = 1; i <= 10; i++) {
  ENTITY_FREQUENCIES.push(i);
}
for (let i = 15; i <= 100; i += 5) {
  ENTITY_FREQUENCIES.push(i);
}

export { ENTITY_FREQUENCIES };
